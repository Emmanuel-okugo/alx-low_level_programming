#include <stdio.h>

/**
 * _puts - entry point
 * @str: character parameter
 * description: a function that prits a string, followed by a new line, 
 * to stdout
 *
 */
void _puts(char *str)
{
	puts(str);
}
