#include "main.h"

/**
 * add - entry point
 * @c: accepts first argument
 * @d: accepts second argumant
 * description: adds two numbers
 * Return: sum
 */

int add(int c, int d)
{
	return(c + d);
}

