#include <stdio.h>

/**
 * mul - entry point
 * @a: first parameter
 * @b: second parameter
 * description: multiplies two integers and return the output
 * Return: a * b
 */

int mul(int a, int b)
{
	return (a * b);
}
